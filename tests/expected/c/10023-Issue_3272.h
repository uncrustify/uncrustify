//#define INLINE_BUSY_WAIT(ticks)            \
//{                                          \
//    volatile size_t busy_wait_counter = 0; \
//    while (busy_wait_counter++ < ticks);   \
//}

// #define INLINE_BUSY_WAIT(ticks)            \
// {                                          \
//     volatile size_t busy_wait_counter = 0; \
//     while (busy_wait_counter++ < ticks);   \
// }
