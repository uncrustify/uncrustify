void f(int n, int m)
{
	int a = 1;
	bool b = a < 10;

	for (int x=0; x<20; ++x) {}
	for (int i=0; i<=n; i+=2) {}
	for (; a==b;) {}
	for (int j=0, k=0; j<n && k<m; ++j) {}

	int e[] = { 1, 2, 3 };
	enum E { X = 1 };
}
