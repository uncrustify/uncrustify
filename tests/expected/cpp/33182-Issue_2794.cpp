int
main()
{
        int i, j, k, l, m, q;

        i = rand();
        k = rand();
        l = rand();
        m = rand();
        j = rand();
        q = i * j + (2 * l) /m - ( 100 * k ) + k * k - i * i + 3000 * j + 1000; /*
                                                                                 * this
                                                                                 * is
                                                                                 * a
                                                                                 * very
                                                                                 * long
                                                                                 * trailing
                                                                                 * c
                                                                                 * comment
                                                                                 */

// the trailing * */ above should be */
}

