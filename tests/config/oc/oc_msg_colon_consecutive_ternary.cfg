# Minimal config to trigger consecutive ternary regression
# When nl_oc_msg_args adds newlines after OC message arguments AND
# pos_conditional is set to lead, the second ternary in consecutive
# OC message arguments breaks incorrectly with ? on its own line.
nl_oc_msg_args = true
pos_conditional = lead
