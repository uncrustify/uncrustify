indent_columns   = 3
indent_with_tabs = 0
# (284)Align continued statements at the '='. Default=True
# If False or the '=' is followed by a newline, the next line is indent one tab.
#indent_align_assign = true

# (527)The span for aligning on '=' in assignments (0=don't align)
align_assign_span = 2
# (528)The threshold for aligning on '=' in assignments (0=no limit)
#align_assign_thresh = 2

# (521)The threshold for aligning variable definitions (0=no limit)
#align_var_def_thresh=1
# (522)The gap for aligning variable definitions.
#align_var_def_gap=2

# (0)The type of line endings. Default=Auto.
# Newlines Unix|Newlines Win|Newlines Mac|Newlines Auto
newlines = cr
