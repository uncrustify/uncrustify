sp_arith                        = force

# c/09605  don't add space before the minus sign in: frames = ((frames + 15) & -16); 
# oc/50609 don't add space before the minus sign in: NSNumber *foo1 = @-1;